{# {% extends 'base.html.twig' %}

{% block title %}New TypeArmements{% endblock %}

{% block body %}
    <h1>Create new TypeArmements</h1>

    {{ include('type_armements/_form.html.twig') }}

    <a href="{{ path('app_type_armements_index') }}">back to list</a>
{% endblock %} #}
{% extends 'base_with_menu.html.twig' %}

{% block title %}Créer type d'armement{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header bg-primary">
            <h3 class="card-title">
                <i class="nav-icon fa fa-plus"></i>
                Créer type d'armement
            </h3>
    </div>
    {% for message in app.flashes('success') %}
    <div class="alert alert-success">
            {{ message }}
    </div>
    {% endfor %}
    <div class="card-body">
            {{ include('type_armements/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
    <div class="card-footer">
        <div class="row text-center">
                <div class="col col-4"><a class="btn btn-sm btn-default" href="{{ path('app_type_armements_index') }}">Retour à la liste</a></div>
                <div class="col col-4"></div>
                <div class="col col-4"></div>
        </div>
    </div>
</div>
{% endblock %}
