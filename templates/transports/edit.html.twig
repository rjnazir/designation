{# {% extends 'base.html.twig' %}

{% block title %}Edit Transports{% endblock %}

{% block body %}
    <h1>Edit Transports</h1>

    {{ include('transports/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_transports_index') }}">back to list</a>

    {{ include('transports/_delete_form.html.twig') }}
{% endblock %}
 #}
{% extends 'base_with_menu.html.twig' %}

{% block title %}Modifier transport{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header bg-primary">
        <h3 class="card-title">
            <i class="nav-icon fa fa-edit"></i>
            Modifier transport
        </h3>
        <div class="card-tools">
            <a href="{{ path('app_transports_new') }}" class="btn btn-sm btn-light">
                <i class="fa fa-plus"></i>
            </a>
        </div>
    </div>
    {% for message in app.flashes('success') %}
    <div class="alert alert-success">
            {{ message }}
    </div>
    {% endfor %}
    <div class="card-body">
        {{ include('transports/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    </div>
    <div class="card-footer">
        <div class="row text-center">
                <div class="col col-4"><a class="btn btn-sm btn-default" href="{{ path('app_transports_index') }}">Retour à la liste</a></div>
                <div class="col col-4">{{ include('transports/_delete_form.html.twig') }}</div>
                <div class="col col-4"></div>
        </div>
    </div>
</div>
{% endblock %}
